{
  "name": "express-tiny-session",
  "description": "express cookie session middleware",
  "repository": {
    "type": "git",
    "url": "https://github.com/dmitriyv/express-tiny-session"
  },
  "version": "1.0.0",
  "keywords": [
    "connect",
    "express",
    "middleware",
    "session"
  ],
  "dependencies": {
    "lodash": "~3.7.0",
    "debug": "~2.1.3",
    "on-headers": "git://github.com/vdmitriy/on-headers.git"
  },
  "files": [
    "HISTORY.md",
    "LICENSE",
    "README.md",
    "index.js"
  ],
  "license": "MIT",
  "devDependencies": {
    "assert": "^1.3.0",
    "cookie-parser": "^1.3.4",
    "express": "^4.12.3",
    "istanbul": "^0.3.13",
    "mocha": "^2.2.4",
    "supertest": "^0.15.0"
  },
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks test/"
  }
}
